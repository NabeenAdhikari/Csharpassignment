namespace MyApp
{
    public static class Area
    {
        public static int Areas(int length, int breadth)
        {
            return length * breadth;
        }

        public static int Perimeter(int length, int breadth)
        {
            return 2 * (length + breadth);
        }
    }
}
